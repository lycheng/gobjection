Objection!
===

A tool use for test with input and output in blackbox

Input can be:

* Kafka msg
* HTTP Request
* Redis operation

Output can be:

* Kafka msg
* HTTP Response
* Database records
* Redis key-value
